#import "../fragments/TorrentSearchResult"

query TorrentContentSearch($input: TorrentContentSearchQueryInput!) {
  torrentContent {
    search(input: $input) {
      ...TorrentSearchResult
    }
  }
}
